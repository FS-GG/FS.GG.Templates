# Generated FS GG UI Claude Code Instructions

@AGENTS.md

Use project-local Claude Code skills in `.claude/skills/` for Spec Kit and selected FS.GG.UI capability workflows. When a feature plan exists, read the active feature plan under `specs/<feature>/plan.md`, matching the generated Codex guidance.

Project settings live in `.claude/settings.json`. User-local settings such as `.claude/settings.local.json` or `~/.claude/settings.json` are optional personal preferences and are not required for generated-project readiness.
