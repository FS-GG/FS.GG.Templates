# Clean consumer proof

Pack the library, restore it from an empty local NuGet source, install `NpmPackage@NpmVersion`, compile with Fable, and run the applicable runtime smoke. This sample deliberately records the two independent package managers.
