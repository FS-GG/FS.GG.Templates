# BindingsProduct

This NuGet package contains curated Fable interop for `NpmPackage@NpmVersion`.
Consumers must separately install that exact npm runtime dependency; this package never republishes it.
