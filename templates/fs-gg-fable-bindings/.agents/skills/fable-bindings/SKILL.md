---
name: fable-bindings
description: Maintain the qualified Babylon Fable bindings workspace.
---

# Fable bindings

Use the pinned Babylon declaration closure, run `npm ci`, `npm run check:drift`, Node and Chromium smoke evidence, and review generated candidates before curating `src/`. Never overwrite maintained interop or advance the declaration lock from generation. Pack and verify a clean NuGet plus separately pinned npm consumer before release.
