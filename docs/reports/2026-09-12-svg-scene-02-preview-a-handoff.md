# SVG-SCENE-02 Preview-A handoff

Date: 2026-09-12. Milestone: **SVG-SCENE-02.7**. This is a local candidate handoff, not a
publication or default-activation receipt.

## Qualified generated consumers

`FS.GG.Workspace.Template` `0.11.0-preview.1` now generates the opt-in SVG foundation with exact
pins to the coherent Rendering `0.29.0-preview.1` candidate axis. The consumer builds only from the
packet's isolated feed. The feed contains `FS.GG.UI.Scene`, its transitive
`FS.GG.UI.KeyboardInput`, `FS.GG.UI.Scene.SvgBrowser`, and the template candidate; no source-project
edge or fallback FS.GG package is admitted.

The existing grid and fractional-coordinate examples still use the retained public surface. An
additional typed `fsgg.svg-document/1` example exercises gradients and stops, affine transforms,
nested clips, alpha and luminance masks, a symbol instance, a local self-hosted font, semantic
selection, canonical serialization, and standalone export through the same candidate API. The
browser observation loads the exported SVG into an isolated iframe and checks local references,
computed gradient paint, font readiness, and non-empty geometry. Noto Sans Latin 400 is the exact
Fontsource 5.3.0 asset selected in M0, SHA-256
`09aee8065d25508f23a4c3d92cd777ac869c52d93fd868a88f025d888a7937d6`; the template carries its
OFL-1.1 notice and makes no network or host-font assumption.

The tactical fixture remains a projection of already captured disclosed values. Its adapter accepts
no undisclosed source object, and a negative witness proves an identity outside the disclosed visible
set cannot enter selection, focus, or the accessible projection. This work did not access S.I.R.

`tests/composition/fable-game/verify-svg-preview-packet.sh` qualifies clean
`--svgFoundation true --lifecycle none` creation, .NET build/tests, Fable compilation, browser
mount/export/reload, and an older retained receiver. The staged adopter preflights its bounded managed
set before writing, preserves authored domain/client files, lifecycle metadata, owner guidance, and
installed skill bytes, and records a content-addressed backup manifest. Collision refusal performs no
write; an injected mid-adoption failure restores the original tree; the explicit rollback command also
restores the tree byte-for-byte. It never reruns the scaffold against the retained workspace.

`tests/composition/fable-game/verify-svg-typed-receivers.sh` independently creates clean and retained
`typed-sdd`/profile-2 receivers. Public `FS.GG.SDD.Cli` 1.7.0 provisions exact Quint 0.32.0 and `lmt`
objects, then authors and inspects offline. Both generated receivers run their normal root build/browser
entry, the full SVG document observation, the retained model tests, deterministic semantic-amendment
run, and real .NET/Fable reducer replay. The original retained subject and additive document subject
remain separate 192-transition corpora; both clean and retained receivers replay the `192 + 192`
relationship and agree on canonical document serialization/export. Wrong tool, wrong profile, stale
binding, collision, and interrupted adoption controls remain separate refusals.

## Exact packet and API evidence

Each exact-head workflow artifact retains all four candidate archives, source commits, archive hashes,
template payload-tree hash, per-package Fable `.fsi` aggregate hashes, feed mapping, release order,
pins, browser observations, backup manifests, and clean/upgrade/refusal evidence. Archive hashes bind
the produced bytes and are not a claim that independent NuGet packs are byte-reproducible. The pinned
Rendering source is `c4e50dcb239ccb62453cdd47505f1a8d1095814e`; the older Templates adoption
baseline is `d19fc1d48647edfebad4a706db64648017fead65`.

Assembly compatibility is inherited from Rendering's exact-head API compatibility gate and the
generated consumers compile against the archived Fable interfaces whose hashes are in each packet.
The public API and consumer evidence is therefore compared on both .NET and Fable axes without an
arbitrary-SVG-import claim. The required promotion order is Scene, KeyboardInput, SvgBrowser, then the
template's immutable pins/package. NuGet.org remains available only for non-FS.GG dependencies in the
local rehearsal.

## M0/M1/M2 reconciliation

| Work package | Handoff outcome and authority boundary |
|---|---|
| M0.1 donor/license and capability audit | **Satisfied for Preview A.** The characterized tactical input stays disclosed-only; the selected Noto asset and OFL notice are retained. No donor access occurred. |
| M0.2 source/test characterization | **Satisfied.** The earlier foundation evidence remains unchanged and these clean/retained consumers add the full selected document surface. |
| M0.3 ownership/package decision | **Satisfied.** Portable document/reducers remain in Scene, browser behavior in SvgBrowser, the required portable keyboard adapter in KeyboardInput, session policy in Game, and generated consumption in Templates. Rendering has no Game dependency. |
| M0.4 geometry/font spike | **Satisfied for the selected Preview-A use.** The self-hosted font is exercised. `polygon-clipping` remains authoring-only, absent from the player graph, with its M3 worker/limit prerequisite unchanged. |
| M0.5 browser/performance subjects | **Satisfied for Preview A** by Rendering [#1285](https://github.com/FS-GG/FS.GG.Rendering/pull/1285) and [#1286](https://github.com/FS-GG/FS.GG.Rendering/pull/1286), including three-browser, real Orca/AT-SPI, and frozen-budget evidence. Unavailable heap/presentation dimensions and physical mobile remain separate later work. |
| M0.6 migration catalog | **Satisfied for this bounded foundation delta.** Clean, retained, collision, interruption, and explicit rollback journeys pass. Product data/replay/keymap migrations remain separate later subjects. |
| M0.7 installed compatibility census | **Satisfied for the two selected local candidate receivers.** Publicly installed candidate receivers remain the SVG-PREVIEW-A operation. |
| M1.1 scene/value contracts | **Satisfied** by Rendering [#1282](https://github.com/FS-GG/FS.GG.Rendering/pull/1282) and [#1283](https://github.com/FS-GG/FS.GG.Rendering/pull/1283), then compiled and exercised here. |
| M1.2 asset/session/extension envelopes | **Satisfied as contract foundations.** Scene asset/extension contracts and Game [#621](https://github.com/FS-GG/FS.GG.Game/pull/621) session declarations compile in their owning evidence. They do not implement or close M5. |
| M1.3 Fable closure | **Satisfied.** The isolated local candidates expose and compile the selected Fable surface; exact interface hashes are retained. |
| M1.4 tactical compatibility | **Satisfied within captured disclosed data.** The unchanged tactical semantic identities, layers, camera, selection, focus, revision, and disclosure controls pass. |
| M1.5 input/editor models | **Satisfied as minimal foundations** by Rendering [#1284](https://github.com/FS-GG/FS.GG.Rendering/pull/1284) and [#1285](https://github.com/FS-GG/FS.GG.Rendering/pull/1285). This does not close complete M3/M4 tooling. |
| M2.1 retained renderer | **Satisfied for Preview A.** Generated consumers retain the existing scene routes and mount the full typed document. |
| M2.2 transforms/camera/picking | **Satisfied for the selected browser contract.** Fractional affine output, retained camera/selection, and the owning three-browser interaction evidence agree. |
| M2.3 style/text/clip/mask/symbol | **Satisfied.** Every selected definition class is exercised here and export/reload preserves its local references and visual predicates. |
| M2.4 accessibility bridge | **Satisfied for Preview A** by the retained semantic controls plus Rendering's actual Orca/AT-SPI receipt. Complete C18/M9–M10 remains later. |

No M1/M2 source obligation remains unfulfilled for the bounded Preview-A candidate. The remaining
prerequisite is operational and intentionally specific: SVG-PREVIEW-A must publish the byte-identical
coherent Rendering archives, publish Templates with immutable public pins, and repeat both clean and
retained journeys from the public feeds as installed receivers before any default/registry activation.

Arbitrary SVG import and authoring remain M3; complete editor/input tooling remains M3/M4; a real session
runtime remains M5; complete presentation/replay/network/scaling, C18/C19, physical-mobile, heap, and
presentation-timestamp qualifications remain at their later milestones. The feature is delivered when
this local handoff merges, while Preview-A publication remains pending and separately authorized.
