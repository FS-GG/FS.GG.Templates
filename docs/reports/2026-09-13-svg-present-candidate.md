# SVG presentation generated candidate

Date: 2026-09-13. Feature: `SVG-PRESENT-01.6`. Publication: false.

The candidate composes the completed producer surfaces into the opt-in generated player while all public
template and package pins remain unchanged. Its packet admits exact archives only from Rendering, Game, and
Audio manifests, verifies every archive hash, then packs Templates once from the committed source revision.

The accepted producer revisions are Rendering `65a64478527cfe12dce5cb401d0a5d6e592abb76`, Game
`91ef49ea638032686ea967083198fd82f2de028e`, and Audio
`3015051f95354f302d2d8b75a072c9ad300e1c6f`.

## Product boundary

- Game remains the authority for session snapshots, save identity, migration acceptance, and autosave state.
- Rendering owns deterministic clip sampling, retained animation frames, IndexedDB transactions, and archive
  validation.
- Audio owns gesture unlock, asset decoding, buses, voices, and Web Audio disposal.
- Templates supplies only product composition, codecs, vector sample content, controls, and qualification.

The player build has no Studio, geometry-worker, polygon-clipping, OpenAL, or native-audio module. Animation,
audio, and storage observations are presentation state and never update gameplay outcomes.

## Qualification

The exact candidate workflow generates direct, SDD-none, SDD-default, typed/profile-2, wizard-adopter, and
retained 0.11 receivers. It verifies collision refusal, interrupted apply, byte-identical rollback, authored
file preservation, locked Fable build, and player bundle isolation. Chromium, Firefox, and WebKit each run the
same installed player journey:

1. wait for IndexedDB readiness;
2. unlock Web Audio through a real button gesture and decode the product sound;
3. advance a movement clip and dispatch one meaningful live cue;
4. seek another clip without replaying the cue;
5. preserve the prior committed save through a quota failure and recover with a valid edit;
6. export a hash-bound archive, replace state, import the archive, and reload the restored save; and
7. settle decorative motion under `prefers-reduced-motion` without a cue.

The local committed-source Fable build and Vite production bundle pass. The installed Chromium journey passes
against locally staged producer archives. The pull-request workflow supplies the exact all-browser and retained
receiver evidence before merge; its uploaded packet and qualification JSON bind all producer revisions,
package hashes, Templates payload hash, route outcomes, and browser observations.

Automated graph evidence proves meaningful audio dispatch and lifecycle. Audibility remains the separately
disclosed listening dimension recorded by the Audio producer; this candidate does not relabel browser graph
automation as a human listening result.
