# SVG Preview A installed public qualification

Date: 2026-09-12. Milestone: **SVG-PREVIEW-A.5**. Templates receiver source:
`71f436c3bde1c23ddf42be01516e7eb850443597`. Exact-head workflow:
[34700376405](https://github.com/FS-GG/FS.GG.Templates/actions/runs/34700376405).

## Public inputs and routes

The qualification used isolated NuGet.org sources and caches. It downloaded and checksum-bound
`FS.GG.Workspace.Template` 0.11.0 at
`41fa91ba1674a4c1140c4054d4e76cff00cd514462dcdb3d9b3e3cdfa22ba4d9` and the retained public
0.10.0 baseline at `69cbed30447e6bd4d221e0ce78060c8245d2744fc3993b4c27368aeeb48d11c8`.
The 0.11.0 tag peeled to `6a66e0a31c33feab4c8f650709b585df6ac3d4c4`; Rendering 0.29.0 remained
bound to `c654a33bb206c6f3aa0a3adb310231a0d54aec63`.

Four installed routes passed:

- direct `dotnet new fs-gg-fable-game --svgFoundation true --lifecycle none`;
- public SDD 1.7.0 with lifecycle `none` and the released fable-game provider;
- public SDD 1.7.0 typed lifecycle with `quint-specification-v1` and exact
  `fsgg-quint-profile/2` tools, including offline author and inspect;
- public `FS.GG.NewSddWorkspace` 0.11.1 creating an SVG-free fable-game/lifecycle-none workspace,
  followed by the released Templates bounded SVG adopter.

The retained route used the actual published Templates 0.10.0 provider descriptor and archive, then
applied the same bounded upgrade. Authored domain/client files, typed lifecycle provenance, owner guidance,
and installed skill bytes remained unchanged. Collision refusal wrote nothing, injected interruption rolled
back atomically, and explicit rollback restored the original tree byte-for-byte.

## Behavior and model evidence

Every route passed the normal root build/test/browser entry, SVG project restore and Fable compile, local
serve, semantic selection and focus, definitions, affine geometry, self-hosted text, canonical serialization,
standalone export and tactical compatibility. Each installed browser observation found the expected three
roots, selected `unit:7`, focusable `unit:11`, gradient, clips, masks, symbol/use, transformed geometry,
ready local font, and an isolated export whose references and gradient paint remained local and valid.

The clean and retained typed receivers separately replayed the unchanged retained-interaction corpus and
the additive document-interaction corpus: **192 + 192 transitions** through both .NET and Fable with no
divergence. The retained semantic amendment was deterministic and exercised pointer id 3. Action mapping,
stale acceptance, wrong order, invalid reference acceptance, lost capture and non-atomic edit mutants were
killed in both runtimes. Wrong tool objects, the wrong profile and stale bindings remained explicit refusal
controls.

## Browser, assistive technology and performance binding

The public archive changed distribution provenance, so Chromium was rerun from the installed public bytes
for all four routes above. NuGet signing adds only `.signature.p7s`; the published payload matches the retained
release payload entry-for-entry, so no Rendering implementation, browser adapter, accessibility bridge or
performance workload byte changed from the accepted Preview-A candidates.

The three-browser, real Orca/AT-SPI and frozen performance/resource receipts from Rendering
[PR #1285](https://github.com/FS-GG/FS.GG.Rendering/pull/1285) and
[PR #1286](https://github.com/FS-GG/FS.GG.Rendering/pull/1286) therefore remain the owning evidence for the
unchanged dimensions, now joined to the installed public archive by the exact payload and source identities
above. Heap measurement, presentation timestamps and physical-mobile observation remain unavailable and are
still later-milestone work. Complete C18/C19 accessibility, arbitrary SVG import, authoring/editor tooling and
session/runtime scaling remain outside Preview A.

SVG stays explicitly selected. Provider activation, lifecycle defaults and general template defaults are
unchanged.
